Rundeck Plugin HTTP Step
========

Gradle Build
=====

    ./gradlew clean build

Artifacts:

* `rundeckapp/build/libs/rundeck-plugin-tasking-0.1.jar`

## Install

Copy the `rundeck-plugin-tasking-0.1.jar` file to the `$RDECK_BASE/libext/` directory inside your Rundeck installation.
